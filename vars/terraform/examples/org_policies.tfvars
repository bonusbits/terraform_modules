org_policies = {
  full_aws_access = {
    # This example is of a managed policy, change to own custom
    description                 = "Allows access to every operation"
    name                        = "FullAWSAccess"
    type                        = "SERVICE_CONTROL_POLICY"
  }
}
