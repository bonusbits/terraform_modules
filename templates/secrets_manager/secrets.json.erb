{
  "<%= $tf_workspace %>": {
    "ssh_keys": {
      <% $ssh_keys.each do |key, values| %>
      "<%= key %>": {
        "private": "<%= values['private'] %>",
        "public": "<%= values['public'] %>"
      },
      <% end %>
    },
    "tfvars": {}
  }
}
